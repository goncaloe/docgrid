---
name: arrancar-etapa
description: Arranca uma etapa do roteiro DocGrid carregando o briefing, o handoff anterior e a documentação relevante, e produzindo um plano de implementação. Usa quando o utilizador começa uma etapa nova, diz "vamos à etapa X", ou invoca /arrancar-etapa com um número.
---

# Arrancar etapa

Segue o procedimento em `.agents/prompts/arrancar-etapa.md`, com **NN = o número da etapa
indicado na invocação** (duas casas: `00`, `03`, `11`): `$ARGUMENTS`.

Lê esse ficheiro agora e cumpre-o na íntegra. É a fonte única do procedimento — não o
reproduzas de memória nem o alteres aqui.
