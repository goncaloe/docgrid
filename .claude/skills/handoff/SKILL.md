---
name: handoff
description: Escreve o relatório de fim de etapa do DocGrid em docs/handoffs/, para que a próxima sessão limpa saiba o que existe e o que ficou por fazer. Usa no fim de uma etapa, quando o utilizador diz que vai fechar a sessão, ou ao invocar /handoff com o número da etapa.
---

# Handoff da etapa

Segue o procedimento em `.agents/prompts/handoff.md`, com **NN = o número da etapa
indicado na invocação** (duas casas: `00`, `03`, `11`): `$ARGUMENTS`.

Lê esse ficheiro agora e cumpre-o na íntegra. É a fonte única do procedimento — não o
reproduzas de memória nem o alteres aqui.
