---
name: implementar-etapa
description: Implementa uma etapa do roteiro DocGrid a partir do plano já aprovado em docs/plans/, numa sessão limpa e sem a conversa que o produziu. Usa quando o utilizador retoma uma etapa já planeada, diz "implementa a etapa X", ou invoca /implementar-etapa com um número.
---

# Implementar etapa

Segue o procedimento em `.agents/prompts/implementar-etapa.md`, com **NN = o número da etapa
indicado na invocação** (duas casas: `00`, `03`, `11`).

Lê esse ficheiro agora e cumpre-o na íntegra. É a fonte única do procedimento — não o
reproduzas de memória nem o alteres aqui.
