{
  "name": "docgrid",
  "version": "0.1.0",
  "private": true,
  "description": "Runner de comandos do DocGrid. Não é um projeto Node: só existe para dar um comando único a quem clona o repositório.",
  "scripts": {
    "infra": "docker compose up -d --wait",
    "up": "npm run infra && node scripts/mvnw.mjs spring-boot:run -Dspring-boot.run.profiles=local",
    "down": "docker compose down -v",
    "test": "node scripts/mvnw.mjs verify",
    "lint": "node scripts/mvnw.mjs spotless:check",
    "format": "node scripts/mvnw.mjs spotless:apply",
    "logs": "docker compose logs -f"
  },
  "engines": {
    "node": ">=20.6"
  }
}
